# Colab install list.
#
# NOTES:
# - bitsandbytes is OMITTED — Colab's triton version breaks its CUDA binary
#   and transformers.integrations.bitsandbytes errors on any transformers import.
#   We use bf16 (not 4-bit).
# - vllm, mergekit, llm_blender are REQUIRED because trl's GRPOTrainer
#   hard-imports them (transitively via callbacks). Even if we set
#   use_vllm=False, the imports run at module load. All three must be present.

transformers==4.46.3
trl==0.15.2
peft==0.13.2
accelerate==1.1.1
datasets==3.1.0
vllm==0.6.6
mergekit
llm_blender
pydantic>=2.7
