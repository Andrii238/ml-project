# transformers 4.46.3 imports has_avx512bf16 from bitsandbytes.functional
# during from_pretrained (even without 4-bit request), so bitsandbytes MUST be
# >= 0.45 or that import fails.
transformers==4.46.3
bitsandbytes>=0.45
trl
peft
accelerate
datasets
mergekit
llm_blender
pydantic
