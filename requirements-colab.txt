# Colab already ships torch, transformers, accelerate, huggingface_hub, datasets.
# We only add what's missing. bitsandbytes is NOT listed — nothing in this project
# uses 4-bit quant (Qwen 1.5B fits on T4 in bf16), and shipping bnb keeps causing
# version-mismatch import errors on Colab.
peft
trl
