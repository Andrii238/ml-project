# Minimum-pinning install. transformers pinned to 4.x because 5.x introduces
# API changes (has_avx512bf16 etc) that break older bitsandbytes builds.
transformers<5.0
trl
peft
accelerate
datasets
mergekit
llm_blender
bitsandbytes
pydantic
