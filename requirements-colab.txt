# Strict pin on transformers because later 4.x builds probe bitsandbytes API
# during from_pretrained even without quantization request, and the newer API
# calls (has_avx512bf16) aren't in older bitsandbytes builds Colab has.
transformers==4.46.3
trl
peft
accelerate
datasets
mergekit
llm_blender
bitsandbytes
pydantic
